# Grid geometry helpers

Reusable, pure-NumPy helpers shared by every grid strategy and by `DataMapper`.
They take plain arrays (and the raw grid-data dict), so they are easy to test in
isolation. Primarily of interest to contributors.

::: sr3kit.grid.geometry
    options:
      show_root_heading: false
      members_order: source
