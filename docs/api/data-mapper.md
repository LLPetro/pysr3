# DataMapper

The property layer: maps SR3 spatial-property arrays onto grid cells using the
`PropGlobalID` / `GlobalCellID` arrays stamped by `GridBuilder`, with optional
bottom-up LGR aggregation.

::: sr3kit.data_mapper.DataMapper
