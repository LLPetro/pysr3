# SR3Indexer

The data-access layer: opens the SR3/HDF5 file and serves indexed metadata and
raw arrays. Use it as a context manager so the file handle is always closed.

::: pysr3.sr3_indexer.SR3Indexer
